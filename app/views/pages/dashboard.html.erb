<table class="table">
<tr>
    <th>Nombre de usuario</th>
    <th>Role</th>
    <th>cambiar role</th>
    <th>Eliminar</th>

</tr>
<% @users.each do |user|  %>
<tr>
    <td><%= user.name %></td>
    <td><%= user.role %></td>
    <td><%= link_to 'Cambiar rol', change_role_path(user.id), method: :post  %></td>
    <td><%= link_to 'eliminar', user_path(user.id), method: :delete  %></td>


</tr>
<% end %>

</table>